mutation CreateReview($recipeId: Int!, $review: String!, $rating: Int!) {
    createReview(
        command: {
            recipeId: $recipeId
            review: $review
            rating: $rating
        }
    ) {
        id
        review
        rating
    }
}