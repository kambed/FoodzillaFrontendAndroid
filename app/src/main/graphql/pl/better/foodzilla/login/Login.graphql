mutation Login($login: String!, $password: String!) {
    login(
        username: $login
        password: $password
    ) {
        token
        customer {
            firstname
            lastname
            username
            email
        }
    }
}